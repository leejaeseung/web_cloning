extends layouts/main

block content
    .profile
        .info
            h1#title #{userName}'s Profile
            if nowUser.userName == userName
                #edit-button
                    a.linkBt(href=routes.editProfile(userName)) 프로필 수정
        img(src=routes.home + imgUrl, alt="프로필을 적용해 주세요.", width="200", height="200")
        p E-mail : #{userEmail}
        