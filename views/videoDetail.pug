extends layouts/main
include mixins/videoBlock

block content
    .video_player
        video(src=routes.home + video.fileUrl, width="1280px". height="720px", controls)
    .video_edit
        h2=video.videoName
        p=video.description
        p=video.views
        a(href=routes.editVideo(video.id)) Edit